# Verified on Python 3.9.6 (PORTRAIT_DL_R3).
# Versions below record this verification environment; upgrade in a separate tested environment.
# No font is bundled. The DXF does not depend on one, but reproducing the PNG
# bytes needs the same faces this build resolved:
#   bold=Arial.ttf / regular=Arial.ttf
# Override with the HANOK_FONT environment variable.
# 2026-09-14: Pillow raised from 11.3.0 to 12.3.0 for security fixes. Pillow 12 needs
# Python 3.10+; a rebuild on Python 3.11.15 kept the DXF, CSV files and checks identical,
# but the archived PNGs are the Pillow 11.3.0 rendering and 12.3.0 renders other bytes.
ezdxf==1.4.2
shapely==2.0.7
Pillow==12.3.0
